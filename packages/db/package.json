{
  "name": "@stocks/db",
  "version": "0.1.0",
  "scripts": {
    "dev": "prisma studio",
    "postinstall": "prisma generate",
    "push": "prisma generate && prisma db push"
  },
  "devDependencies": {
    "prisma": "^3.14.0"
  },
  "dependencies": {
    "@prisma/client": "^3.14.0"
  }
}
